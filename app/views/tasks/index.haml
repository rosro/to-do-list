%h1 Hello, Rails!
= link_to 'My Task', controller: 'tasks'
= link_to 'New task', new_task_path
%table
  %tr
    %th Title
    %th Text
    %th
  - @tasks.each do |task|
    %tr
      %td= task.title
      %td= task.text
      %td= link_to 'Show', task_path(task)
